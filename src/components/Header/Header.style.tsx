import { styled } from 'styled-components';

export const StyledHeaderContainer = styled.header`
    z-index: 10;
    display: flex;
    flex-direction: column;
`;
StyledHeaderContainer.displayName = 'Header';
